#ifndef Secrets_h
#define Secrets_h

// ==== Arrays to store multiple Wi-Fi credentials ====

const char* ssids[] = {
// here write ssids of your WiFi routers (separated by comma)
};

const char* passwords[] = {
// here write passwords of your WiFi routers in the same order as ssids (separated by comma)
};

// ==== Telegram settings ====

const String BOT_TOKEN = "";       // Telegram bot token (fill in)
const String CHAT_ID   = "";          // Telegram chat ID to send messages to (fill in)

#endif